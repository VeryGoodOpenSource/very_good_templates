name: dependency_tightener
description: Ensures all the packages in the directory have tight dependencies.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.4.0

dev_dependencies:
  very_good_analysis: ^5.1.0

name: very_good_flutter_plugin

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    paths:
      - .github/workflows/very_good_flutter_plugin.yaml
      - "very_good_flutter_plugin/**"
      - .editorconfig
      - .swiftformat
      - .clang-format
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/very_good_flutter_plugin.yaml
      - "very_good_flutter_plugin/**"
      - .editorconfig
      - .swiftformat
      - .clang-format
    branches:
      - main

jobs:
  brick:
    defaults:
      run:
        working-directory: very_good_flutter_plugin/

    runs-on: ubuntu-latest

    strategy:
      matrix:
        platform:
          - android
          - ios
          - linux
          - macos
          - web
          - windows

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v6

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.x"

      - name: 🧱 Mason Make
        run: |
          dart pub global activate mason_cli
          dart pub global activate very_good_cli
          mason get
          mason make very_good_flutter_plugin -c config.json --on-conflict overwrite --platforms ${{ matrix.platform }} -o very_good_flutter_plugin_output

      - name: 📦 Install Dependencies
        run: very_good packages get --recursive very_good_flutter_plugin_output

      - name: ✨ Check Dart Formatting
        if: false
        run: dart format --set-exit-if-changed very_good_flutter_plugin_output

      - name: ✨ Check Kotlin Formatting
        if: matrix.platform == 'android'
        run: |
          curl -sSLO https://github.com/pinterest/ktlint/releases/download/1.5.0/ktlint
          chmod a+x ktlint
          ./ktlint --editorconfig="${GITHUB_WORKSPACE}/.editorconfig" "very_good_flutter_plugin_output/**/src/main/kotlin/**/*.kt" || {
            echo "::error::Kotlin formatting issues found. Run 'ktlint --format' locally to fix."
            exit 1
          }

      - name: ✨ Check C++ Formatting
        if: matrix.platform == 'linux' || matrix.platform == 'windows'
        run: |
          sudo apt-get install -y clang-format
          CPP_FILES=$(find very_good_flutter_plugin_output -type f \( -name "*.h" -o -name "*.cpp" -o -name "*.cc" \) | grep -v -E "(Pods|DerivedData|build|.dart_tool)" || true)
          if [ -n "$CPP_FILES" ]; then
            ERRORS=0
            for file in $CPP_FILES; do
              if ! clang-format --style=file:"${GITHUB_WORKSPACE}/.clang-format" --dry-run --Werror "$file" 2>/dev/null; then
                echo "::error file=$file::Formatting issues found in $file"
                ERRORS=$((ERRORS + 1))
              fi
            done
            if [ $ERRORS -gt 0 ]; then
              echo "::error::C++ formatting issues found. Run 'clang-format -i <file>' locally to fix."
              exit 1
            fi
          fi

      - name: 🕵️ Analyze
        if: false
        run: dart analyze --fatal-infos --fatal-warnings very_good_flutter_plugin_output

      - name: 🧪 Run Tests
        run: |
          cd very_good_flutter_plugin_output
          very_good test -j 4 --recursive --optimization --coverage --test-randomize-ordering-seed random

      - name: 📑 Combine Code Coverage
        run: |
          sudo apt-get -y install lcov
          very_good packages get --recursive tool/merge_coverage
          cd very_good_flutter_plugin_output
          dart run ../tool/merge_coverage/merge_coverage.dart

      - name: 📊 Check Code Coverage
        uses: VeryGoodOpenSource/very_good_coverage@v3
        with:
          path: very_good_flutter_plugin/very_good_flutter_plugin_output/coverage/lcov.info

  swift-format:
    defaults:
      run:
        working-directory: very_good_flutter_plugin/

    runs-on: macos-latest

    strategy:
      matrix:
        platform:
          - ios
          - macos

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v6

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.x"

      - name: 🧱 Mason Make
        run: |
          dart pub global activate mason_cli
          dart pub global activate very_good_cli
          mason get
          mason make very_good_flutter_plugin -c config.json --on-conflict overwrite --platforms ${{ matrix.platform }} -o very_good_flutter_plugin_output

      - name: 📥 Install SwiftFormat
        run: brew install swiftformat

      - name: ✨ Check Swift Formatting
        run: |
          # Find Swift files in the platform-specific directories
          SWIFT_DIRS=""
          if [ "${{ matrix.platform }}" = "ios" ]; then
            SWIFT_DIRS=$(find very_good_flutter_plugin_output -type d -name "ios" | head -5)
          elif [ "${{ matrix.platform }}" = "macos" ]; then
            SWIFT_DIRS=$(find very_good_flutter_plugin_output -type d -name "macos" | head -5)
          fi

          if [ -n "$SWIFT_DIRS" ]; then
            for dir in $SWIFT_DIRS; do
              if [ -d "$dir" ]; then
                swiftformat --config "${GITHUB_WORKSPACE}/.swiftformat" --lint "$dir" || {
                  echo "::error::Swift formatting issues found. Run 'swiftformat .' locally to fix."
                  exit 1
                }
              fi
            done
          fi

name: very_good_core

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    paths:
      - .github/workflows/very_good_core.yaml
      - "very_good_core/**"
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/very_good_core.yaml
      - "very_good_core/**"
    branches:
      - main

jobs:
  brick:
    defaults:
      run:
        working-directory: very_good_core/

    runs-on: ubuntu-latest

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v6

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.x"

      - name: 🧱 Mason Make
        run: |
          dart pub global activate mason_cli 0.1.3
          dart pub global activate very_good_cli 1.1.0
          mason get
          mason make very_good_core -c config.json --on-conflict overwrite -o very_good_core_output

      - name: 📦 Install Dependencies
        run: very_good packages get --recursive very_good_core_output

      - name: ✨ Check Formatting
        run: dart format --set-exit-if-changed very_good_core_output

      - name: 🕵️ Analyze
        run: dart analyze --fatal-infos --fatal-warnings very_good_core_output

      - name: 🧪 Run Tests
        run: |
          cd very_good_core_output
          very_good test -j 4 --recursive --optimization --min-coverage 100 --test-randomize-ordering-seed random

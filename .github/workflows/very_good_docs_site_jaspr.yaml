name: very_good_docs_site_jaspr

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    paths:
      - .github/workflows/very_good_docs_site_jaspr.yaml
      - "very_good_docs_site_jaspr/**"
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/very_good_docs_site_jaspr.yaml
      - "very_good_docs_site_jaspr/**"
    branches:
      - main

jobs:
  brick:
    runs-on: ubuntu-latest

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v7

      - name: 🎯 Setup Dart
        uses: dart-lang/setup-dart@v1.8.1
        with:
          sdk: "3.13.0"

      - name: 🧱 Mason Make
        run: |
          dart pub global activate mason_cli 0.1.3
          mason get
          mason make very_good_docs_site_jaspr -c very_good_docs_site_jaspr/config.json --on-conflict overwrite -o very_good_docs_site_jaspr_output

      - name: 📦 Install Dependencies
        run: dart pub get --directory very_good_docs_site_jaspr_output

      - name: ✨ Check Formatting
        run: dart format --set-exit-if-changed very_good_docs_site_jaspr_output

      - name: 🕵️ Analyze
        run: dart analyze --fatal-infos --fatal-warnings very_good_docs_site_jaspr_output

      - name: 👷 Build website
        run: |
          dart pub global activate jaspr_cli 0.23.4
          cd very_good_docs_site_jaspr_output
          jaspr build
